workflows:
  android-app:
    name: Android App
    instance_type: mac_mini_m1
    environment:
      flutter: stable
    scripts:
      - name: Build Android App
        script: ./gradlew build
    artifacts:
      - app/build/outputs/**/*.apk