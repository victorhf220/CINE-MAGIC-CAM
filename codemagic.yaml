workflows:
  android-app:
    name: Android App
    instance_type: mac_mini_m1
    scripts:
      - name: Build Android App
        script: |
          cd $CM_BUILD_DIR
          ./gradlew assembleRelease
    artifacts:
      - app/build/outputs/**/*.apk